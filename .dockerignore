# ============================================================
# 构建上下文排除清单（放在项目根，因为构建上下文 = 项目根）
# ============================================================
# ⚠️ 先搞清楚它管什么：
#   .dockerignore 管的是「哪些文件会被打包发给 Docker 守护进程」（上下文）
#   它 **不** 决定「哪些文件进镜像」—— 那是 Dockerfile 里 COPY 决定的
#
#   那它有什么用？—— venv 1.3G、node_modules 几百 M，不加这个清单，
#   每次 build 都要把这一坨整个传给守护进程，慢到离谱。
# ============================================================

# Python 虚拟环境（本机的，容器里不用）
venv/
__pycache__/
*.pyc
.pytest_cache/

# 前端依赖和构建产物
node_modules/
frontend/dist/

# 🔑 密钥：绝对不进镜像。
#    运行时用 docker run --env-file .env 或 compose 的 env_file 注入
.env

# 文档 / 笔记 / 编辑器配置（体积小，但没必要进上下文）
docs/
notebook/
.claude/
*.md

# 操作系统垃圾
.DS_Store
Thumbs.db
data/faiss_index/